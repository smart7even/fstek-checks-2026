**Цель**: Выявление и блокирование запросов к API СПО, не соответствующих утвержденной спецификации.

**Регламентирование**: Не предъявляются.

Описание реализации, действий и результатов при испытаниях:

| Требование | Действие | Результат |
|------------|----------|-----------|
| В СПО определена спецификация API; запросы автоматически проверяются на формат, структуру и ограничения и блокируются до воздействия на приложение. | Отправить на https://example.ru/api запрос по спецификации и запросы с полями вне схемы, неверным типом и недопустимой структурой. | Успех: Соответствующий запрос принят; остальные отклонены до прикладной логики. |
| Перечень и спецификация API пересматриваются ежегодно или при изменении API на предмет недокументированных, устаревших и неиспользуемых методов. | Сопоставить утвержденную спецификацию с фактически доступными внешним пользователям методами https://example.ru/api; открыть дату пересмотра на example.ru/admin. | Успех: Недокументированные и устаревшие методы недоступны; пересмотр выполнен ежегодно либо при изменении API. |
| **Условия: К1.** Усиление 1: пересмотр спецификации при изменении кода или конфигурации приложений, реализующих API. | Внести контрольное изменение конфигурации API и проверить, что спецификация пересмотрена в том же цикле. | Успех: Пересмотр выполнен при изменении кода или конфигурации. |

**Параметры безопасности в интерфейсе администратора безопасности (СПБ.5):**

Все перечисленные параметры должны быть доступны для оперативного редактирования из интерфейса СПО (`example.ru/admin`) администратору безопасности, с ограничениями, исключающими ослабление политик безопасности ниже значений, установленных методикой и требованиями к СПО (мера СПБ.5).

- утвержденная спецификация API (OpenAPI или эквивалент);
- правила блокирования запросов вне спецификации;
- дата последнего пересмотра перечня и спецификации API.

Требования к усилению:

1) пересмотр перечня и спецификации API при изменении кода или конфигурации приложений, реализующих данные API;
2) автоматический анализ трафика внешних API на предмет недокументированных, устаревших и неиспользуемых методов.

| Мера защиты информации | К3 | К2 | К1 |
|------------------------|----|----|-----|
| ЗПИ.3 | + | + | + |
| Усиление ЗПИ.3 |  | | 1 |
