**Цель**: Ограничение возможности исчерпания ресурсов СПО запросами пользователей и приложений.

**Регламентирование**: Не предъявляются.

Описание реализации, действий и результатов при испытаниях:

| Требование | Действие | Результат |
|------------|----------|-----------|
| Установлены ограничения на число одновременных сеансов и запросов в секунду с одного адреса (идентификатора) для веб-интерфейсов, API и интерфейса администрирования. Значения задает оператор. | 1. Открыть на example.ru/admin заданные оператором лимиты.<br><br>2. Превысить лимит запросов к example.ru/public и https://example.ru/api с одного клиента; выполнить одиночный запрос с другого клиента. | 1. Успех: Лимиты заданы и доступны администратору безопасности.<br><br>2. Успех: Избыточные запросы отклонены; одиночный запрос другого клиента выполнен. |
| При реализации ограничения обеспечивается мониторинг и выявление аномального поведения (ЗОО.3). Квоты на файлы исключают заполнение хранилища одним пользователем (СПБ.12, СПБ.14). | Во время превышения лимита открыть журнал РСБ и сведения о квотах загрузки файлов на example.ru/admin. | Успех: Превышение лимита зафиксировано; квоты файлов действуют. |

**Параметры безопасности в интерфейсе администратора безопасности (СПБ.5):**

Все перечисленные параметры должны быть доступны для оперативного редактирования из интерфейса СПО (`example.ru/admin`) администратору безопасности, с ограничениями, исключающими ослабление политик безопасности ниже значений, установленных методикой и требованиями к СПО (мера СПБ.5).

- максимальное число одновременных соединений (сеансов) с одного адреса/УЗ;
- максимальное число запросов в секунду для public, API и admin;
- квоты размера и числа файлов на пользователя;
- пороги аномальной нагрузки для оповещения.

Требования к усилению:

Не предъявляются.

| Мера защиты информации | К3 | К2 | К1 |
|------------------------|----|----|-----|
| ЗОО.5 | + | + | + |
| Усиление ЗОО.5 |  | |  |
