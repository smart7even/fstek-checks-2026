**Цель**: Обеспечение доступности прикладных сервисов СПО для внешних пользователей при атаках, направленных на отказ в обслуживании.

**Регламентирование**: Не предъявляются.

Описание реализации, действий и результатов при испытаниях:

| Требование | Действие | Результат |
|------------|----------|-----------|
| **Субъект: Внешние пользователи.**<br><br>Прикладные сервисы, предоставляемые внешним пользователям, публикуются так, что из внешней сети доступен только предназначенный контур (`example.ru/public`). Административный контур остается в ЗКС (СПБ.1). Размещение серверов в ДМЗ (МСЭ.2) — требование контура информационной системы и проверяется по ОРД/схеме, не подменой испытания СПО. | 1. С внешней точки открыть example.ru/public.<br><br>2. С внешней точки открыть example.ru/admin.<br><br>3. С сегмента ЗКС открыть example.ru/admin. | 1. Успех: Прикладной сервис доступен.<br><br>2. Успех: Административная панель извне ЗКС недоступна.<br><br>3. Успех: Вход в панель из ЗКС возможен. |
| **Субъект: Внешние пользователи / анонимные.**<br><br>СПО не принимает объекты, способные исчерпать хранилище прикладного сервиса (СПБ.12 – СПБ.14). | 1. Превысить допустимый размер файла и число файлов на example.ru/public.<br><br>2. Отправить архив сверх лимита несжатого размера (СПБ.13). | 1. Успех: Загрузка отклонена; сервис отвечает другим клиентам.<br><br>2. Успех: Архив отклонен до распаковки. |
| Очистка входящего трафика от составляющих DoS на границе ДМЗ выполняется средством оператора и (или) провайдера. Это не функция интерфейса СПО; на испытаниях СПО фиксируется наличие услуги в ОРД и то, что СПО сохраняет доступность public-контура при прикладных лимитах ЗОО.5. | 1. Сверить в ОРД наличие фильтрации/очистки DoS на стороне оператора или провайдера.<br><br>2. На example.ru/admin открыть прикладные лимиты ЗОО.5 и подтвердить, что они заданы для public и API. | 1. Успех: Услуга очистки подтверждена документально (либо зафиксировано ее отсутствие — для контура это отдельный дефект инфраструктуры, не СПО).<br><br>2. Успех: Прикладные лимиты СПО заданы и доступны администратору безопасности. |

**Параметры безопасности в интерфейсе администратора безопасности (СПБ.5):**

Все перечисленные параметры должны быть доступны для оперативного редактирования из интерфейса СПО (`example.ru/admin`) администратору безопасности, с ограничениями, исключающими ослабление политик безопасности ниже значений, установленных методикой и требованиями к СПО (мера СПБ.5).

- признак публикации административного контура только из ЗКС;
- максимальный размер принимаемого файла и число файлов на пользователя;
- лимиты несжатого размера и числа файлов в архиве;
- прикладные лимиты запросов public/API (совместно с ЗОО.5).

Требования к усилению:

Не предъявляются.

| Мера защиты информации | К3 | К2 | К1 |
|------------------------|----|----|-----|
| ЗОО.1 | + | + | + |
| Усиление ЗОО.1 |  |  |  |
