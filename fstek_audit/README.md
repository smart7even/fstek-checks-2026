# fstek_audit

Audit-engine layout for FSTEK checks.

Use from repository root:

```bash
./check_all.sh --class K3
./check_all.sh --measure ИАФ.3
./check_all.sh --list
```

See `docs/architecture.md` for the layout and `docs/statuses.md` for verdict semantics.
