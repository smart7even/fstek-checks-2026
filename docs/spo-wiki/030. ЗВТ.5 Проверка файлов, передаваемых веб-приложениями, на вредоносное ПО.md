**Цель**: Обнаружение вредоносного программного обеспечения в файлах, передаваемых веб-приложениями СПО, и реагирование на обнаружение.

**Регламентирование**: Не предъявляются.

Описание реализации, действий и результатов при испытаниях:

| Требование | Действие | Результат |
|------------|----------|-----------|
| Все файлы, передаваемые веб-приложениями, проверяются на вредоносное ПО в соответствии с АВЗ.1. | 1. Загрузить на example.ru/admin тестовый индикатор (EICAR).<br><br>2. Повторить на example.ru/public, если загрузка внешним пользователям предусмотрена.<br><br>3. Загрузить допустимый файл разрешенного типа. | 1. Успех: Файл не сохранен либо помещен в карантин; выполнено реагирование.<br><br>2. Успех: Файл отклонен либо внешняя загрузка не предусмотрена.<br><br>3. Успех: Допустимый файл принят после проверки. |
| Проверка включает все файлы в теле запроса, а также скрипты и данные, поступающие в веб-приложение. | Передать несколько файлов в одном запросе, один из которых содержит признак вредоносного ПО; передать скрипт в теле запроса. | Успех: Проверены все составляющие запроса; зараженный объект не принят. |
| Исключен автоматический запуск переданных файлов; исполняемые файлы и маскирование форматов отклоняются. Сжатые архивы проверяются до распаковки (СПБ.13). | Загрузить исполняемый файл, файл с несоответствием типа содержимому и архив, превышающий допустимый несжатый размер или число файлов. | Успех: Исполняемые и маскированные файлы отклонены; архив проверен до распаковки и отклонен при превышении квот. |

**Параметры безопасности в интерфейсе администратора безопасности (СПБ.5):**

Все перечисленные параметры должны быть доступны для оперативного редактирования из интерфейса СПО (`example.ru/admin`) администратору безопасности, с ограничениями, исключающими ослабление политик безопасности ниже значений, установленных методикой и требованиями к СПО (мера СПБ.5).

- признак обязательной антивирусной проверки загружаемых файлов;
- перечень разрешенных типов файлов;
- максимальный размер файла и число файлов на пользователя (СПБ.12, СПБ.14);
- лимиты несжатого размера и числа файлов в архиве (СПБ.13);
- действие при обнаружении (отклонение, карантин, уведомление).

Требования к усилению:

1) обеспечивается возможность проведения ретроспективного анализа файлов.

| Мера защиты информации | К3 | К2 | К1 |
|------------------------|----|----|-----|
| ЗВТ.5 | + | + | + |
| Усиление ЗВТ.5 |  | |  |
