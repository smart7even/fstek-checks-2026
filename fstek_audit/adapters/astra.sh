#!/bin/bash
# adapters/astra.sh - OS adapter placeholder for future OS-specific evidence collection.
# Current checks still use shared read-only helpers from fstek_audit/core/common.sh.

fstek_adapter_name() { printf '%s\n' 'astra'; }
