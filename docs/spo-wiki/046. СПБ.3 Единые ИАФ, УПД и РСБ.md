**Цель**: Для всех подсистем или АС СПО реализованы единые аутентификация, авторизация, управление правами и регистрация событий.

**Регламентирование**: Не предъявляются.

Описание реализации, действий и результатов при испытаниях:

| Требование | Действие | Результат |
|------------|----------|-----------|
| **Субъект: Внутренний непривилегированный.**<br><br>После входа на example.ru/admin повторная идентификация в каждую АС не требуется. | Войти на example.ru/admin и обратиться к нескольким АС/подсистемам. | Успех: Повторный вход в каждую АС не требуется. |
| Изменение права в одной точке применяется во всех подсистемах. | Отозвать право на объект одной АС и повторить доступ к той же функции в другой АС. | Успех: Измененное право применено везде. |
| События всех подсистем попадают в единый журнал РСБ. | Выполнить вход, отказ в доступе и привилегированное действие в разных АС; открыть журнал РСБ. | Успех: События всех подсистем в едином журнале с одним идентификатором субъекта. |

**Параметры безопасности в интерфейсе администратора безопасности (СПБ.5):**

Все перечисленные параметры должны быть доступны для оперативного редактирования из интерфейса СПО (`example.ru/admin`) администратору безопасности, с ограничениями, исключающими ослабление политик безопасности ниже значений, установленных методикой и требованиями к СПО (мера СПБ.5).

- единый каталог пользователей и ролей;
- единый журнал РСБ для всех подсистем.

Требования к усилению:

Не предъявляются.

| Мера защиты информации | К3 | К2 | К1 |
|------------------------|----|----|-----|
| СПБ.3 | + | + | + |
| Усиление СПБ.3 |  |  |  |
