**Цель**: Исключение несанкционированного доступа субъектов (пользователей и приложений), взаимодействующих с СПО посредством API, не прошедших аутентификацию.

**Регламентирование**: Не предъявляются.

Описание реализации, действий и результатов при испытаниях:

| Требование | Действие | Результат |
|------------|----------|-----------|
| Определен перечень API, для которых требуется контроль доступа. Идентификация и аутентификация — по ИАФ.1, ИАФ.3; управление доступом — по УПД.1 – УПД.6. | Вызвать защищенный метод https://example.ru/api с действительным, недействительным и отсутствующим аутентификатором; вызвать метод вне роли пользователя и приложения. | Успех: Без аутентификации — 401; вне роли — 403; разрешенный метод выполнен. |
| Ограничение параллельных сеансов — по УПД.7; завершение по неактивности — по УПД.8; проверка прав на каждый запрос, кроме УПД.9. | Превысить лимит сеансов API; выдержать таймаут неактивности; вызвать метод после отзыва права. | Успех: Сеансы сверх лимита ограничены; неактивный сеанс завершен; после отзыва права запрос отклонен. |
| Установлены ограничения на частоту и объем обращений к API и пресечение подбора средств аутентификации. | Превысить частоту обращений к https://example.ru/api и направить последовательность перебора аутентификаторов. | Успех: Избыточные обращения отклонены; подбор пресечен автоматически. |

**Параметры безопасности в интерфейсе администратора безопасности (СПБ.5):**

Все перечисленные параметры должны быть доступны для оперативного редактирования из интерфейса СПО (`example.ru/admin`) администратору безопасности, с ограничениями, исключающими ослабление политик безопасности ниже значений, установленных методикой и требованиями к СПО (мера СПБ.5).

- перечень API с контролем доступа;
- права пользователей и приложений (client_id) на методы API;
- лимит параллельных сеансов API и таймаут неактивности;
- лимиты частоты и объема обращений; порог пресечения подбора.

Требования к усилению:

Не предъявляются.

| Мера защиты информации | К3 | К2 | К1 |
|------------------------|----|----|-----|
| ЗПИ.2 | + | + | + |
| Усиление ЗПИ.2 |  | |  |
